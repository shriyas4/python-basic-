#to print a calendar
import calendar
y = int(input("Enter the year you want the calendar for: "))
m = int(input("Enter the number of the month: "))
print(calendar.month(y,m))
#to print the calendar for the entire year
for j in range(1,13):
    print(calendar.month(y,j))
